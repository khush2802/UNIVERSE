# Copy to .env.local for development.

# Show development fixtures in a production build. Leave unset.
# Fixtures are not real projects and must never appear on the live site.
NEXT_PUBLIC_ALLOW_FIXTURES=

# --- Added in later chunks -------------------------------------------
# DATABASE_URL=            # chunk 12
# GITHUB_TOKEN=            # chunk 13, raises the API rate limit
# ANTHROPIC_API_KEY=       # chunk 15
# OWNER_PASSWORD_HASH=     # chunk 16
